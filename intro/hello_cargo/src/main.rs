/*

    - Cargo is rusts build system / package manager
    - On build automatically creates executable in debug directory
    - run with ./target/debug/hello_cargo or with cargo run to auto compile then run
    - cargo check to compile but not build exe
    - cargo build --release for optimizations

*/
fn main() {
    println!("Hello, world!");
}
